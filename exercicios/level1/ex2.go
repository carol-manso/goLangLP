
import (
	"fmt"
)

var x int
var y string
var z bool

func main() {
	fmt.Println(x, y, z) //resultado: 0 " " false -> valores 0, o que o compilador atribui às variáveis quando não atribuimos nada a elas.

}
